<%= form_for(@jerryism) do |f| %>
  <% if @jerryism.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jerryism.errors.count, "error") %> prohibited this jerryism from being saved:</h2>

      <ul>
      <% @jerryism.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :created_by %><br>
    <%= f.text_field :created_by %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
